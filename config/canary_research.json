{
  "version": 1,
  "mode": "shadow_only",
  "hypotheses": ["roll", "roll_veto", "roll_confirm", "roll_conflict", "twap_trend"],
  "roll_threshold_usd_per_second": 0.10,
  "adverse_return_bps": 1.0,
  "confirmation_return_bps": 0.5,
  "confirmation_flow_imbalance": 0.20,
  "hold_seconds": [15, 30],
  "latency_seconds": [1, 3],
  "execution_models": ["taker", "maker_trade_through_proxy"],
  "shares": 5,
  "quote_lifetime_seconds": 5,
  "max_quote_age_ms": 1500,
  "max_quote_skew_ms": 250,
  "max_new_quote_wait_ms": 2500,
  "max_entry_slippage_ticks": 1,
  "adverse_slippage_stress_ticks": 1,
  "max_virtual_position_usd": 5,
  "max_virtual_session_loss_usd": 2,
  "min_independent_windows": 300,
  "min_utc_days": 7,
  "min_filled_attempts": 100,
  "min_valued_exit_fraction": 0.99,
  "selection_note": "Fixed before examining fresh production returns. Earlier development clips inform mechanism only. No refit, rebates, live executor or automatic activation. Thresholds are fixed in source; this is the audit registry, not a runtime override."
}
